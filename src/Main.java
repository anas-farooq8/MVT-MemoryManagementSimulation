import Screens.Window;

// Main class that serves as the entry point of the program
public class Main {
    public static void main(String[] args) {
        // Create an instance of the Window class with the title "MVT"
        Window window = new Window("MVT");
        window.init();
    }
}